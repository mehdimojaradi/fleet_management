# How To Run Tests

1. Install nodejs >= 20
2. Run `git clone https://github.com/mehdimojaradi/fleet_management.git`
3. Run `npm install`
4. Type `cd cypress`
5. Run `npm run test` 
6. If any test fails, see the `screenshots` and `videos` directories to track the bug

